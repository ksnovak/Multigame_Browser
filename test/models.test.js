import assert from 'assert';
import chai from 'chai';

const should = chai.should();

describe('Models', () => {
  describe('Game', () => {
    it('is game');
  });
  describe('Stream', () => {
    it('is stream');
  });
  describe('Errors', () => {
    it('is error');
  });
  describe('QueryOptions', () => {
    it('is query options');
  });
});

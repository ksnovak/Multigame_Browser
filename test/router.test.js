import assert from 'assert';
import chai from 'chai';

const should = chai.should();

describe('Router', () => {
  describe('/api', () => {
    describe('/games', () => {
      it('exists');
    });
    describe('/streams', () => {
      it('exists');
    });
  });
});
